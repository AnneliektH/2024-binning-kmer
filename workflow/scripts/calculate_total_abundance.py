# imports
import sourmash

# Open signature file per contig (sig.contig)

# open signature file of all contigs // or all reads? (sig.all)

# loop through all signatures in sig.contig

# for each hash value in sig.contig per contig, compare to hash values in sig.all

# write out contig, hash value, abundance hash value in sig.all

